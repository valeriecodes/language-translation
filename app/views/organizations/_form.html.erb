<%= form_for(@organization) do |f| %>
  <%= render partial: "shared/error_messages", object: @organization %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
